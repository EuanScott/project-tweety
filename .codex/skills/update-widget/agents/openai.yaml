interface:
  display_name: "Update Widget"
  short_description: "Safely evolve existing shared widgets"
  default_prompt: "Use $update-widget on <widget name or path> to <change>; optional successor: <name>."

policy:
  allow_implicit_invocation: true
