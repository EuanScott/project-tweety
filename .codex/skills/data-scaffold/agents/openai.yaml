interface:
  display_name: "Data Scaffold"
  short_description: "Scaffold data layer files"
  default_prompt: "Use $data-scaffold for <feature_name> in <folder_key>, implementing <repository_operations> with <required_artifacts>; include the existing domain contract or curl and response sample when applicable."
policy:
  allow_implicit_invocation: false
