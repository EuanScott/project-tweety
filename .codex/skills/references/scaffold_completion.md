# Scaffold Completion Gate

Finish only when the selected manifest exists, no target was overwritten, and
the diff contains no out-of-scope artifact or secret. Generated outputs must be
current when their inputs changed. Required formatting, analysis, code
generation, and targeted or proportionate broader tests must pass.
