interface:
  display_name: "Write ADR"
  short_description: "Draft evidence-backed Project Tweety ADRs"
  default_prompt: "Use $write-adr to draft a proposed ADR for <decision>, including the chosen option, owner, and any relevant repository context."
policy:
  allow_implicit_invocation: true
