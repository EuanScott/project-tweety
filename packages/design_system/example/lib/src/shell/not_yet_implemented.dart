import 'package:material_ui/material_ui.dart';

/// Placeholder for a gallery entry whose showcase hasn't been built yet.
class const NotYetImplemented({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Showcase not yet implemented.'));
  }
}
