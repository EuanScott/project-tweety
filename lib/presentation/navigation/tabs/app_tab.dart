/// The top-level tabs rendered by the app shell.
///
/// The enum order is intentionally aligned with the bottom navigation
/// destinations and the `StatefulShellRoute` branches.
enum AppTab { home, cards, settings }
